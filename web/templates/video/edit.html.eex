<h2>Edit video</h2>

<%= render "form.html",
      action: video_path(@conn, :update, @video),
      categories: @categories,
      changeset: @changeset %>

<%= link "Back", to: video_path(@conn, :index) %>
